# Assets

`milad-shabani.jpg` is the creator photo shown in the dashboard's footer box
(`dashboard/index.html`). If you fork this project and swap in your own
photo under the same filename, it updates automatically. If the file is
ever missing, the dashboard falls back to a plain initials badge — no
broken image icon either way.
